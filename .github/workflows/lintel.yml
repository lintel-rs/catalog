name: Lintel

on:
  push:
    branches: [master]
  pull_request:

permissions:
  checks: write
  contents: read

jobs:
  lintel:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: lintel-rs/action@v0
